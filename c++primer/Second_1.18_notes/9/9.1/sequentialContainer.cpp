#include <iostream>
#include <vector>
#include <deque>
#include <list>
#include <forward_list>
#include <array>
#include <string>
using namespace std;

int main() {
    vector<int> arr(10, 1);
    cout << str(vector<string>::value_type()) << endl;
}