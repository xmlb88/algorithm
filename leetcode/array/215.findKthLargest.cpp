#include <iostream>
#include <queue>
#include <vector>
using namespace std;

// ¿ìËÙÅÅĞò Ñ¡Ôñ
int findKthLargest(vector<int>& nums, int k) {
    srand((unsigned)time(NULL));
    int n = nums.size() - k;

}

int quickSortPartition(vector<int>& nums, int k, int l, int r) {
    int pos = 
}

int random_partition(vector<int>& nums, int l, int r) {
    int id = rand() % (r - l + 1)
}