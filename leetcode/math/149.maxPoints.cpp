#include <iostream>
#include <vector>
#include <queue>
#include <unordered_map>
using namespace std;

// TODO:
int maxPoints(vector<vector<int>>& points) {
    int n = points.size();
    unordered_map<pair<double, double>, int> m;
    for (int i = 0; i < n; ++i) {
        for (int j = i + 1; j < n; ++j) {

        }
    }
}